const registerRoute = (app) => {
  // Daftarkan router disini
};

const registerRouteSocketIo = (app, io) => {
  // Daftarkan router yang ingin memakai socket.io disini
};

module.exports = {
  registerRoute,
  registerRouteSocketIo,
};
